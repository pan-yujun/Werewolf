# 模块 02 - 状态管理与异步流程控制 - 实现文档

本实现文档精确记载了状态管理与异步流程拦截机制的具体代码文件、行号映射以及操作细节。

---

## 一、 Jotai 状态原子与持久化实现

在 [src/store/game-machine.ts](file:///d:/Develop/ClaudeProjects/Werewolf/src/store/game-machine.ts) 中：

### 1.1 状态原子定义
*   **物理入口**：`export const gameStateAtom = atom(...)` -> [Line 514](file:///d:/Develop/ClaudeProjects/Werewolf/src/store/game-machine.ts#L514)
*   **状态初始化与加载**：
    *   在加载游戏时，会调用 `loadPersistedGameState()` 函数从 `localStorage` 中同步拉取、反序列化并做类型验证：[Line 308-401](file:///d:/Develop/ClaudeProjects/Werewolf/src/store/game-machine.ts#L308-L401)
    *   `loadPersistedGameState` 会做 TTL (生存时间) 检验，若判定缓存已超 24 小时 (以 `STATE_TTL_MS = 24 * 60 * 60 * 1000` 为准，定义于 [Line 35](file:///d:/Develop/ClaudeProjects/Werewolf/src/store/game-machine.ts#L35))，则执行 `removeItem` 清空并置空。
*   **数据落盘**：
    *   每次调用 `gameStateAtom` 写入新状态时，在自定义原子的 `write` 属性中都会自动调度对 `saveGameState(nextState)` [Line 403-455](file:///d:/Develop/ClaudeProjects/Werewolf/src/store/game-machine.ts#L403-L455) 的调用。
    *   在底层，真正的浏览器存储写入由 `writeStateToLocalStorage(persisted)` [Line 466-486](file:///d:/Develop/ClaudeProjects/Werewolf/src/store/game-machine.ts#L466-L486) 执行。
    *   清除持久化缓存由 `clearPersistedGameState()` [Line 488-500](file:///d:/Develop/ClaudeProjects/Werewolf/src/store/game-machine.ts#L488-L500) 执行。

---

## 二、 FlowToken 与异步拦截实现

在 [src/lib/game-flow-controller.ts](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/game-flow-controller.ts) 中：

### 2.1 流程控制协议与结构
*   **令牌契约定义**：`export interface FlowToken` -> [Line 6-9](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/game-flow-controller.ts#L6-L9)
    ```typescript
    export interface FlowToken {
      isValid(): boolean;
    }
    ```
*   **令牌产生器**：`getToken(): FlowToken` -> [Line 17-30](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/game-flow-controller.ts#L17-L30)
    *   在每次发起异步调用前，通过主控实例 `flowController.current.getToken()` 捕获状态切片。
*   **活性判定器**：`isTokenValid(token: FlowToken): boolean` -> [Line 31-33](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/game-flow-controller.ts#L31-L33)
*   **异步流强行截断 (令牌废弃)**：`interrupt(): void` -> [Line 35-37](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/game-flow-controller.ts#L35-L37)
    *   通过自增内部活跃流水号（`activeFlowId`），使得先前发放出去的所有 Token 在调用 `isValid()` 时都会由于流水号不一致而判定为 `false`。

### 2.2 中央编排器的防竞态挂载与检查
在核心主 Hook [src/hooks/useGameLogic.ts](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/useGameLogic.ts) 中：
*   **挂载点**：`const flowController = useRef(new AsyncFlowController())` -> [Line 245](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/useGameLogic.ts#L245)
*   **拦截重置操作**：当用户或程序自动触发 `restartGame()` 重置游戏时，首先执行 `flowController.current.interrupt()` [Line 2333](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/useGameLogic.ts#L2333)，瞬间切断上一局未完成的所有 AI 异步响应。
*   **阶段流转拦截**：在开始推进下一阶段前调用 `flowController.current.interrupt()` [Line 1765](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/useGameLogic.ts#L1765)。
*   **异步校验示例**：
    *   在处理白天 AI 玩家的发言流之前获取并绑定 Token：
        ```typescript
        const token = flowController.current.getToken();
        ```
    *   在生成完发言、写入 dialogue 状态以及变更发言座位号前，强行调用 `token.isValid()` 进行活性验证，拦截任何过期的长耗时回调。

---

## 三、 维护约定

> [!WARNING]
> 本实现文档中记载的行号是后续代码排查的关键线索。凡是触发了相关源文件的重构、函数移动或大规模删除/合并操作，**必须在此文档中同步校准行号**。代码的提交与文档的更新必须具备原子性。
