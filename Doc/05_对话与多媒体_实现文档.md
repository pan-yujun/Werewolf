# 模块 05 - 对话、语音与多媒体交互 - 实现文档

本实现文档精确记录了流式段落提取、音频串行播放队列及打字机同步协同所涉及的代码物理文件和行数指针。

---

## 一、 流式发言解析器实现 (`StreamingSpeechParser`)

在 [src/lib/streaming-speech-parser.ts](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/streaming-speech-parser.ts) 中：
*   **物理入口**：`export class StreamingSpeechParser` -> [Line 20](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/streaming-speech-parser.ts#L20)
*   **核心构造**：
    *   构造器接收一个 `onSegment` 回调函数，每当检测并提取出一个完整的非空字符句段，就立刻将其向上传递：[Line 40-42](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/streaming-speech-parser.ts#L40-L42)
*   **字符拼接与边界切分**：
    *   外部通过不断调用 `feed(chunk)` 输入大模型 SSE 的片断数据：[Line 44-78](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/streaming-speech-parser.ts#L44-L78)
    *   在函数内部，程序正则检索中文 `。！？\n` 及英文句读，遇到标点后提取缓冲区内容并执行 `onSegment`，然后清空缓冲继续拼接后续字符流。
    *   在流式响应完全终结时，外部调用 `flush()` 方法强制将残留于缓冲区内的字符组装并输出：[Line 80-87](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/streaming-speech-parser.ts#L80-L87)

---

## 二、 串行音频调度队列实现 (`AudioManager`)

在 [src/lib/audio-manager.ts](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/audio-manager.ts) 中：
*   **物理入口**：`class AudioManager` -> [Line 18](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/audio-manager.ts#L18)
    *   作为单例导出，挂载在 `window` 对象上供全局调用：`export const audioManager = ...` -> [Line 184-188](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/audio-manager.ts#L184-L188)
*   **任务推入队列**：
    *   `addTask(task: Omit<AudioTask, "audio">): void` -> [Line 45-64](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/audio-manager.ts#L45-L64)
        *   调用此方法将一个语音文本或旁白加入 `queue` 中，若当前无活跃音轨，则立即调起播放。
*   **串行流动执行**：
    *   `playNext(): Promise<void>` -> [Line 91-140](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/audio-manager.ts#L91-L140)
        *   核心的出队与播放驱动器。在创建 HTMLAudioElement 时，为其强行绑定 `onended` 事件处理函数，监听音频的播放完成事件，以驱动 `playNext()` 再次递归触发，形成串行连续流：[Line 106-125](file:///d:/Develop/ClaudeProjects/Werewolf/src/lib/audio-manager.ts#L106-L125)

---

## 三、 对话预取与流式推送协同实现

在 [src/hooks/game-phases/useDayPhase.ts](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/game-phases/useDayPhase.ts) 中：
*   **前置预取器**：
    *   `const prefetchNextAISpeech = useCallback(...)` -> [Line 341-396](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/game-phases/useDayPhase.ts#L341-L396)
        *   当前玩家在发言时，在后台异步预热加载并执行下一位 AI 玩家的提示词装配与大模型请求，缓存其 Segment 数组。
*   **流式发言与播报主循环**：
    *   `const runAISpeech = useCallback(...)` -> [Line 468-700](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/game-phases/useDayPhase.ts#L468-L700)
        *   控制 AI 发言流。它检测预取缓存，若无缓存则调起 `generateAISpeechSegmentsStream`。
        *   通过自定义 `StreamingSpeechParser` 绑定 `onSegmentReceived` 回调，将提取出的 Segment 同步递给 `audioManager.addTask` 以合成 TTS 语音，并写回 Jotai 状态树推进 UI 的打字机显现。

---

## 四、 协同打字机实现 (`useTypewriter`)

在 [src/hooks/useTypewriter.ts](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/useTypewriter.ts) 中：
*   **打字机入口**：`export function useTypewriter(...)` -> [Line 15](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/useTypewriter.ts#L15)
*   **打字速率演算**：
    *   打字机通过监听传入文本长度动态计算输出延时：[Line 30-55](file:///d:/Develop/ClaudeProjects/Werewolf/src/hooks/useTypewriter.ts#L30-L55)
    *   在有 TTS 音频任务绑定时，用文本字数除以音频总播放秒数，动态匹配 TTS 声音的振幅速率，达到视听的统一：
        ```typescript
        const speed = Math.max(minSpeed, Math.min(maxSpeed, audioDuration / text.length));
        ```

---

## 五、 维护约定

> [!WARNING]
> 本实现文档中记载的行号是后续代码排查的关键线索。凡是触发了相关源文件的重构、函数移动或大规模删除/合并操作，**必须在此文档中同步校准行号**。代码的提交与文档的更新必须具备原子性。
